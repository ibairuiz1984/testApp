specVersion: "v1"
serviceId: "vue-tests-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vue-tests-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
